export const metadata = {
    title: 'Client Journey | 99 Financial Inc.',
    description: 'Our structured 5-step planning process: from corporate discovery through strategy design, implementation coordination, and ongoing governance.',
};

export default function ClientJourneyLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return children;
}
